package service

func FileHead(fileID, chunkIndex string) {
	//chunkPath := filepath.Join(TempDir, fileId, chunkIndex)
	//
	//if _, err := os.Stat(chunkPath); err == nil {
	//	c.Status(http.StatusOK)
	//} else {
	//	c.Status(http.StatusNotFound)
	//}
}
